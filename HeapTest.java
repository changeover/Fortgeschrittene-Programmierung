public class HeapTest {
    public static void main(String[] args) {
        Heap <float> heap[] = new Heap<>();
        public void populate()
        {
            for (int i = 1; i <= n; i++)
            {
                heap[i] = (100.0*Math.random());
            }
        }
    }
}
